# CCConnect 桌面聊天设计规范

## 1. 视觉主题与整体气质

CCConnect 应该呈现出一种接近微信 Windows 客户端的桌面聊天工具气质：安静、克制、高效、熟悉，尽量像系统原生应用，而不是强调品牌感的展示型页面。界面不依赖渐变、大阴影、悬浮卡片或复杂装饰来制造“高级感”，而是通过稳定的分栏、清晰的信息层级，以及低负担的长时间使用体验来建立品质感。

整体界面的核心是一个典型的三段式桌面工作区：

- 左侧是窄而稳定的全局功能栏
- 中间是会话 / 会话记录列表
- 右侧是主聊天与传输工作区

整个产品的情绪基底应该是柔和浅灰，而不是纯白或高对比科技风。层次主要通过轻微的明度差、细分割线和统一间距来建立，而不是通过明显卡片感来堆叠。绿色只作为关键状态色出现，用于“我发送的内容”“在线”“可发送”“成功进行中”等语义，不应泛滥成整套主题色。

**关键特征**
- 典型桌面 IM 三栏布局，强调左到右的工作流
- 浅灰系统级界面，细分割线，极轻层次
- 以微信式浅绿色为唯一主强调色
- 信息密度高，但不显拥挤或压迫
- 以列表、气泡、分区为主，弱化卡片语言
- 使用中文 UI 友好的无衬线字体体系
- 聊天气泡是主视觉节奏，而不是数据面板
- 动效短、轻、克制，接近桌面原生程序

## 2. 色彩系统与角色定义

### 主强调色
- **发送气泡绿** (`#95ec69`)：用于“我发送的消息气泡”，是最典型、最容易让人联想到微信桌面端的颜色。
- **动作绿** (`#07c160`)：用于在线状态、发送按钮、进度正向状态、聚焦后的正反馈控件。
- **深一级绿色** (`#06ad56`)：用于动作绿的 hover / pressed 状态。

### 中性色表面
- **窗口灰** (`#f5f5f5`)：应用主背景、聊天画布基底。
- **侧栏灰** (`#ededed`)：最左侧功能栏及辅助工具区域背景。
- **面板灰** (`#f7f7f7`)：会话列表、顶部栏、输入区背景。
- **白色表面** (`#ffffff`)：接收气泡、输入框、菜单、轻量浮层。
- **悬停灰** (`#e8e8e8`)：列表行、按钮、功能项悬停态。
- **选中行灰** (`#dcdcdc`)：当前选中会话行背景。
- **弱填充灰** (`#f0f0f0`)：输入底、标签、状态胶囊、时间胶囊。

### 边界与分割
- **主分割线灰** (`#d9d9d9`)：纵向和横向主分割线。
- **次分割线灰** (`#e5e5e5`)：列表内部、辅助区域内部的轻分割线。
- **输入描边灰** (`#cfcfcf`)：输入控件或弱边界容器使用。

### 文字颜色
- **主文字** (`#111111`)：标题、消息正文、重要信息。
- **次文字** (`#666666`)：会话预览、说明文字、次级信息。
- **弱文字** (`#999999`)：时间、占位文案、提示性信息。
- **反白文字** (`#ffffff`)：放在强动作绿按钮或深色背景上的文字。

### 语义色
- **错误红** (`#fa5151`)：传输失败、危险提示、未读红点。
- **警告橙** (`#fa9d3b`)：网络不稳定、会话即将过期等提醒。
- **链接蓝** (`#576b95`)：路径、帮助、引用类文本链接，可少量使用。

### 使用原则
- 中性色应覆盖至少 85% 的界面面积。
- 发送气泡绿主要用于“我方气泡”和少量关键操作态。
- 不要在同一块 UI 中同时大面积使用两种绿色。
- 错误红要克制使用，这样未读和失败状态才有辨识度。
- 优先用背景层次区分状态，不要过分依赖边框。

## 3. 字体与排版规则

### 字体族
- **主字体**：`PingFang SC`
- **Windows 优先回退**：`Microsoft YaHei`
- **通用回退**：`Noto Sans SC`, `Segoe UI`, `system-ui`, `sans-serif`

整个应用建议只用一套无衬线字体体系。CCConnect 是偏工具型、偏操作型的产品，不需要通过字体个性来制造品牌戏剧性。层级主要依赖字号、字重、留白和位置关系来建立。

### 排版层级

| 角色 | 字号 | 字重 | 行高 | 用法 |
|------|------|------|------|------|
| 窗口标题 | 15px | 600 | 1.3 | 聊天标题、主面板标题 |
| 区块标题 | 14px | 600 | 1.4 | 次级标题、列表分组标题 |
| 正文大号 | 15px | 400 | 1.6 | 聊天内容、长文本内容 |
| 正文标准 | 14px | 400 | 1.5 | 会话预览、表单内容 |
| 正文小号 | 13px | 400 | 1.45 | 说明文字、内联元信息 |
| 说明字 | 12px | 400 | 1.4 | 时间、状态说明、空状态提示 |
| 标签字 | 11px | 500 | 1.3 | 状态胶囊、角标、紧凑标签 |

### 原则
- 默认正文建议固定在 `14px` 或 `15px`，更小字号只用于元信息。
- 聊天内容优先使用 `15px`，保证长时间阅读的舒适度。
- 不要使用大段粗体。`600` 仅用于标题或需要强调的关键数字。
- 会话列表可以积极截断，但当前聊天标题应尽量完整展示。
- 时间、速度、文件大小等数字信息要保持对齐和格式统一。

## 4. 组件样式规则

### 应用外壳
- 结构：`72px` 图标功能栏 + `280px` 会话列表列 + 右侧自适应聊天区
- 背景：窗口灰 (`#f5f5f5`)
- 分割：`1px solid #d9d9d9`
- 阴影：应用内部尽量不使用阴影；若有，仅允许外层窗口保留系统级轻阴影

整个外壳应该被感知为一个连续的桌面应用，而不是三个拼接的卡片区块。

### 左侧功能栏
- 宽度：`72px`
- 背景：侧栏灰 (`#ededed`)
- 结构：顶部头像 / 品牌，中部导航图标，底部辅助操作
- 图标尺寸：`22px` 到 `24px`
- 激活态：绿色圆点、绿色描边，或轻微更深的中性底
- 悬停态：切换到悬停灰 (`#e8e8e8`)
- 角标：小型红点或红色数字圆点

左栏应稳定、紧凑、图标化，不要做成大按钮菜单。

### 会话列表列
- 宽度：`280px`
- 背景：面板灰 (`#f7f7f7`)
- 顶部：搜索框 + 可选快捷筛选
- 行高：默认 `72px`，紧凑模式 `64px`
- 选中态：选中行灰 (`#dcdcdc`)
- 悬停态：悬停灰 (`#e8e8e8`)
- 头像：`40px`，可为圆形，也可为 `10px` 圆角方形
- 第一行主文本：`15px`, `600`
- 第二行预览：`13px`, `#666666`
- 时间：`12px`, `#999999`，右对齐
- 未读角标：红色胶囊，最小高度 `18px`

每一行的扫描优先级应保持稳定：

- 左侧是头像
- 主标题在第一行
- 会话预览在第二行
- 时间与未读状态固定在右侧

### 聊天顶部栏
- 高度：`64px`
- 背景：面板灰 (`#f7f7f7`)
- 下边线：`1px solid #d9d9d9`
- 标题：`15px`, `600`
- 副标题 / 在线状态：`12px`, `#999999`
- 右侧操作：图标按钮，点击热区不少于 `32px`

顶部栏只负责定位当前对象，不应该抢走聊天内容的注意力。

### 聊天消息区
- 背景：窗口灰 (`#f5f5f5`)
- 左右内边距：`24px`
- 上下内边距：`20px`
- 文本气泡最大宽度：桌面下约为内容区的 `62%`
- 单条消息间距：`8px`
- 不同发送组 / 时间组之间间距：`16px`

不要给整个消息区再套一层卡片。聊天本身就应该直接“铺”在主画布上。

### 时间分隔条
- 对齐：水平居中
- 字号：`12px`
- 颜色：`#999999`
- 上间距：`24px`
- 下间距：`16px`
- 可选样式：弱填充灰 (`#f0f0f0`) 的胶囊背景，圆角 `999px`，内边距 `6px 10px`

时间分隔应像轻微停顿，而不是强分节线。

### 接收消息气泡
- 背景：白色表面 (`#ffffff`)
- 文本：主文字 (`#111111`)
- 圆角：`8px`
- 内边距：`10px 14px`
- 边框：仅在对比不够时加 `1px solid #ececec`
- 阴影：无

接收气泡应轻、薄、干净，尽量不产生厚重感。

### 发送消息气泡
- 背景：发送气泡绿 (`#95ec69`)
- 文本：主文字 (`#111111`)
- 圆角：`8px`
- 内边距：`10px 14px`
- 阴影：无
- 对齐：右侧

发送气泡是界面里最明确的品牌记忆点，但它的突出只应该来自颜色，而不是额外装饰。

### 头像规则
- 聊天流中标准尺寸：`32px`
- 会话列表标准尺寸：`40px`
- 形状：圆形或 `8px` 到 `10px` 圆角方形
- 与气泡间距：`8px`
- 同一发送者连续发送时，可省略重复头像以减少视觉噪音

### 输入区 / 发送区
- 整体高度：`136px` 到 `168px`
- 背景：面板灰 (`#f7f7f7`)
- 上边线：`1px solid #d9d9d9`
- 工具栏：图标式，单个操作热区 `36px`
- 文本输入框：白色或极浅中性底，不要做厚边框表单感
- 占位文字：`#999999`
- 发送按钮：只有在有内容可发送时才显示为绿色高亮

输入区要像聊天窗口的一部分，而不是独立表单模块。

### 文件传输卡片 / 气泡
- 容器背景：接收侧使用白底，发送侧使用轻微偏绿底
- 圆角：`10px`
- 内边距：`12px`
- 文件图标区域：`40px`
- 标题：`14px`, `600`
- 元信息行：文件大小、速度、来源、剩余时间，使用 `12px`
- 进度条高度：`4px`
- 进度填充：动作绿 (`#07c160`)

对于 CCConnect，这类传输卡片首先要“好扫读”，其次才是“像聊天内容”。

### 会话状态胶囊
- 高度：`24px`
- 圆角：`999px`
- 内边距：`0 10px`
- 字体：`11px`, `500`
- 中性态：弱填充灰底 + 次文字
- 成功态：浅绿底 + 动作绿文字
- 失败态：浅红底 + 错误红文字

### 搜索框
- 高度：`36px`
- 背景：白色表面或弱填充灰
- 圆角：`8px`
- 默认无边框
- 占位文字：弱文字 (`#999999`)
- 聚焦态：细而轻的绿色或蓝绿色描边

## 5. 布局原则

### 主体网格
- 全局结构：`72 / 280 / auto`
- 若未来需要详情面板，优先采用覆盖式或临时展开，而不是长期固定四栏
- 聊天区内部结构：顶部栏 / 可滚动消息区 / 输入区

### 间距系统
- 基础单位：`4px`
- 推荐刻度：`4, 8, 12, 16, 20, 24, 32`
- 列表内部微间距：`8px`
- 气泡外间距：`8px`
- 面板内边距：`16px` 到 `24px`

### 留白哲学
- 应把空间主要留给右侧聊天工作区，而不是浪费在外层装饰。
- 左栏和中栏要偏紧凑，右侧消息阅读区可以更舒展。
- 留白优先用来区分“消息组”“时间组”“内容段落”，而不是装饰区域。

### 形状语言
- 默认圆角：`8px`
- 紧凑控件：`6px`
- 标签 / 胶囊：`999px`
- 同一屏内避免混用过多不同圆角等级

### 对齐原则
- 会话列表中，标题、预览、时间、未读要形成稳定扫描路径。
- 聊天气泡沿发送者边缘对齐，而不是居中排版。
- 顶栏操作区和输入区工具栏应遵循统一图标栅格。

## 6. 层次、状态与动效

### 层次模型

| 层级 | 表现方式 | 用途 |
|------|----------|------|
| Level 0 | 纯平，无阴影 | 主界面、聊天画布、面板底 |
| Level 1 | 仅使用分割线 | 顶栏、区域边界、列表项分区 |
| Level 2 | 轻微背景变化 | hover 行、hover 图标按钮 |
| Level 3 | 轻量浮层 | 菜单、弹出选择器、上下文面板 |

这个风格不依赖大量阴影堆层次。层级应该主要来自分区、对齐和色阶变化。

### 交互状态
- Hover：背景提升一个中性色阶
- Active：使用更深一点的中性底或选中行灰
- Focus：细描边，推荐低饱和的动作绿
- Disabled：降低对比，但不能降低到影响可读性

### 动效规则
- Hover 过渡：`120ms` 到 `180ms`
- 抽屉 / 菜单过渡：`180ms` 到 `220ms`
- 新消息出现：轻微淡入并 `translateY(4px)` 收束
- 避免弹簧感和玩具感动效，保持桌面工具软件的利落感

## 7. 应做与避免

### 应做
- 以浅灰中性色作为整个产品的基底
- 让绿色只承担发送、在线、成功、可操作等高价值语义
- 让聊天内容成为右侧主舞台的绝对核心
- 保持列表扫描节奏稳定、可预测
- 用细分割线代替大面积卡片边框
- 让界面适合长时间连续使用，不产生视觉疲劳
- 保持图标简单、克制、接近系统风格

### 避免
- 不要把界面做成数据大屏或营销首页
- 不要在日常 UI 中使用渐变、玻璃拟态、炫彩背景
- 不要在导航、标题、内容区同时大面积铺绿色
- 不要使用过大的标题浪费桌面空间
- 不要给气泡和列表项加厚重阴影
- 不要在核心应用壳层里混入衬线字体
- 不要把用户习惯横向扫描的内容居中排布

## 8. 响应式策略

### 断点建议

| 名称 | 宽度 | 行为 |
|------|------|------|
| 大桌面 | `>= 1440px` | 标准三栏，消息区更宽松 |
| 标准桌面 | `1200px - 1439px` | 默认布局，会话列保持 `280px` |
| 紧凑桌面 | `1024px - 1199px` | 会话列收窄到 `240px`，内边距略收紧 |
| 平板 / 窄窗口 | `768px - 1023px` | 左栏文字弱化，会话列支持收起 |
| 移动兜底 | `< 768px` | 改成单栏导航模式，不长期并排展示列表和聊天 |

### 响应式优先级
- 优先保住消息可读性，再去压缩外围栏位。
- 窄屏时先缩会话列表，不要先缩聊天字体。
- 无论在什么宽度下，都必须保持“发送 / 接收气泡”区分明显。
- 紧凑模式下也要保证点击热区不少于 `36px`。

## 9. 针对 CCConnect 的补充建议

CCConnect 不只是聊天客户端，它还是设备间文件与文本的传输工具，所以在沿用微信式桌面聊天风格时，需要做一些功能化修正：

- 文件传输状态应尽量以内联消息形式出现在聊天流中，而不是默认藏进独立仪表盘。
- 设备在线状态、会话码、传输进度应优先用紧凑标签、提示行、轻量状态块表达。
- “连接设备”“发送文件”“接收文件”“会话记录”这些能力可以出现在左栏或筛选层，但主工作区仍应保持聊天式连续体验。
- 长文本、传输卡片、系统通知都应遵守同一套气泡节奏，避免产品像多个子系统拼在一起。

## 10. 建议使用的 CSS Token

```css
:root {
  --cc-window: #f5f5f5;
  --cc-sidebar: #ededed;
  --cc-panel: #f7f7f7;
  --cc-surface: #ffffff;
  --cc-hover: #e8e8e8;
  --cc-active-row: #dcdcdc;
  --cc-divider: #d9d9d9;
  --cc-divider-soft: #e5e5e5;
  --cc-text: #111111;
  --cc-text-secondary: #666666;
  --cc-text-tertiary: #999999;
  --cc-bubble-out: #95ec69;
  --cc-green: #07c160;
  --cc-green-deep: #06ad56;
  --cc-danger: #fa5151;
  --cc-warning: #fa9d3b;
  --cc-link: #576b95;
  --cc-radius-sm: 6px;
  --cc-radius-md: 8px;
  --cc-radius-lg: 10px;
  --cc-shadow-float: 0 8px 24px rgba(0, 0, 0, 0.08);
}
```

这套 token 故意保持精简。这个风格的质量来自一致性和克制，而不是来自庞杂的主题变量系统。
